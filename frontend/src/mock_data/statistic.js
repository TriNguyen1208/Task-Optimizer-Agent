import { Statistics } from "../@types/statistics";

const mock_statistic = {
    tasks_complete: 4.5,
    total_hour_worked: 5.0,
    avg_hour_task: 10.0,
    completion_rate: 95
}
export {mock_statistic}